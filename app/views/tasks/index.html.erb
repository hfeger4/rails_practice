<h1>Tasks#index</h1>
<p>Tasks:</p>

<ul>
  <% @tasks.each do |task| %>
  <li>
    <p><h1><%= task.title %></h1></p>
    <p><%= task.details %></p>
    <%= link_to 'Edit', edit_task_path(task) %>
    <%= link_to 'Delete', task_path(task), method: :delete %>
  </li>
  <% end %>
</ul>

<%= link_to 'New Task', new_task_path %>
